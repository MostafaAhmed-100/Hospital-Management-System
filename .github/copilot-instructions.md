# Copilot Instructions

## Project Guidelines
- User plans to use Swagger/OpenAPI in the future. Currently removed Microsoft.AspNetCore.OpenApi and Microsoft.OpenApi packages due to .NET 10.0.10 source generator bug, but these should be re-added once the .NET runtime is updated to a version that fixes the IOpenApiMediaType.Example readonly property issue.